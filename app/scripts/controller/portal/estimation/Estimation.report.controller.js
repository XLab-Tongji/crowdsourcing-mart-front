'use strict';

app.controller('EstimationReportController', ['$scope', '$state', '$rootScope', 'AlertTool', 'ToasterTool',
    'EstimationReportFactory', 'SessionFactory', 'SessionService','Upload', function ($scope, $state, $rootScope, AlertTool, ToasterTool,
        EstimationReportFactory, SessionFactory, SessionService,Upload) {

        init();

        function init() {
            // $scope.nextStep = nextStep;         
        }

        

    }]);



